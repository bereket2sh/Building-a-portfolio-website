import { defineConfig } from 'vite'

export default defineConfig({
  base: '/Building-a-portfolio-website/',  // Must match exactly
  // OR
  // base: '/repository-name/',  // If using github.io/repository-name
}) 